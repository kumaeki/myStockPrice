import React, { FC } from 'react';
import App from '../src/components/App';

const Index = () => {
    return (
        <React.StrictMode>
            <App />
        </React.StrictMode>
    );
};

export default Index;
