import React, { FC } from 'react';
import StockList from '../src/stocklist/StockList';

const Index = () => {
    return (
        <React.StrictMode>
            <StockList />
        </React.StrictMode>
    );
};

export default Index;
